/** NEXT-11 — shared metadata defaults. Route files extend these, never restate them. */
export const SITE = {
  name: 'Al-Asad',
  /** Fallback description; localised copy comes from SSOT-07 at render time. */
  description: 'Ethnic apparel, stitched and unstitched.',
} as const;
