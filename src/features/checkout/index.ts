/**
 * STRUCT-04 / STRUCT-06 — the checkout's SERVER-side barrel. Client Components
 * import from `./contract` instead.
 */
export { fetchOrder, fetchQuote, placeOrder } from './api/checkout-server';
